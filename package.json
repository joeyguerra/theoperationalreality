{
  "name": "theoperationalreality",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "start": "node --env-file-if-exists=.env node_modules/.bin/hubot -a @hubot-friends/hubot-service-discovery -n jbot",
    "test": "node --test",
    "docker-build": "./docker-build-k3d.sh",
    "local-deploy": "kubectl config use-context ${KUBE_CONTEXT:-k3d-local} && kubectl apply -f charts/web/deployment.yaml -n ${KUBE_NAMESPACE:-default}",
    "push": "node --run docker-build && node --run local-deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@hubot-friends/hubot-service-discovery": "^2.0.2",
    "hubot": "^14.0.1",
    "juphjacs": "github:joeyguerra/juphjacs"
  }
}
